import { Staatliches, Ysabeau, Khand } from 'next/font/google';

export const staatliches = Staatliches({
  weight: '400',
  subsets: ['latin'],
});

export const ysabeau = Ysabeau({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const khand = Khand({
  weight: '400',
  subsets: ['latin'],
});
